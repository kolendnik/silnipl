{% extends 'base.html.twig' %}

{% block title %}Account{% endblock %}

{% block body %}
{{ parent() }}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit">Save</button>
    {{  form_end(form) }}
{% endblock %}
